# Canonical Data Model

The former single-file data model is split by section. The section map is in
[`docs/data-model/00-index.md`](data-model/00-index.md).

This path is retained only so `docs/...` `section N` references in ADR text,
changelog, and other historical records still resolve. Do not add content here.
