# Architecture reviews

One report per reviewed subject, named `docs/reviews/architecture/<subject>.json`
(see `docs/agent-contracts.md`). A new review never overwrites an earlier one;
include the subject in the filename. Reports are evidence; the current
architecture is `docs/architecture/`.
