# Architecture Decisions

This document has moved. Its content is split across [`docs/decisions/`](decisions/00-index.md).

This path is retained only so historical references in `docs/implementation/**`,
`docs/reviews/**`, archived plans, and ADR text still resolve. Do not add content
here.
