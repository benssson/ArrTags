using MediaBrowser.Model.Plugins;

namespace ArrTags.Configuration;

/// <summary>
/// The ArrTags plugin configuration.
/// </summary>
/// <remarks>
/// Configuration fields, validation, and immutable snapshots are introduced by
/// the plugin configuration and operational-limits foundation tasks.
/// </remarks>
public class PluginConfiguration : BasePluginConfiguration
{
}
